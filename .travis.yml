sudo: required
language: c

os: linux
dist: bionic

install:
  - sudo add-apt-repository ppa:team-gcc-arm-embedded/ppa -y
  - sudo apt update
  - sudo apt install gcc-arm-embedded -y

jobs:
  include:
    - stage: thread
      script: .travis/build_thread.sh
